---
---
<div id="registration-modal" class="modal" aria-hidden="true" data-open="false">
  <div class="modal-overlay"></div>
  <div class="modal-content">
    <button class="close-btn" aria-label="Close modal">&times;</button>
    
    <!-- Navigation Tabs for Modal -->
    <div class="modal-tabs">
      <button class="tab-btn active" data-target="new-registration">New Registration</button>
      <button class="tab-btn" data-target="already-registered">Already Registered?</button>
    </div>

    <!-- MAIN REGISTRATION FLOW -->
    <div id="new-registration" class="tab-content active">
      <div id="step-1" class="step active">
        <h2>Registration Type</h2>
        <div class="options">
          <button class="type-btn" data-type="individual">
            <span class="icon">👤</span>
            <h3>Individual</h3>
            <p>Register as a solo participant.</p>
          </button>
          <button class="type-btn" data-type="team">
            <span class="icon">👥</span>
            <h3>Team</h3>
            <p>Register with your squad (2-4 members).</p>
          </button>
        </div>
      </div>

      <div id="step-2" class="step">
        <h2 id="form-title">Individual Registration</h2>
        <form id="reg-form">
          <div class="form-grid">
            <div class="field">
              <label for="full-name">Full Name</label>
              <input type="text" id="full-name" required>
            </div>
            
            <div class="field" id="team-name-field" style="display:none;">
              <label for="team-name">Team Name</label>
              <input type="text" id="team-name">
            </div>

            <!-- Team Members Fields (Hidden for Individual) -->
            <div class="field team-member-field" style="display:none;">
               <label for="member-2">Team Member 2 Name</label>
               <input type="text" id="member-2">
            </div>
            <div class="field team-member-field" style="display:none;">
               <label for="member-3">Team Member 3 Name</label>
               <input type="text" id="member-3">
            </div>
            <div class="field team-member-field" style="display:none;">
               <label for="member-4">Team Member 4 Name</label>
               <input type="text" id="member-4">
            </div>

            <div class="field">
              <label for="email">Email ID</label>
              <input type="email" id="email" required>
            </div>

            <div class="field">
              <label for="phone">Phone Number</label>
              <input type="tel" id="phone" required>
            </div>

            <div class="field">
              <label for="college">College Name</label>
              <input type="text" id="college" required>
            </div>

            <div class="field">
              <label for="dept">Department</label>
              <input type="text" id="dept" required>
            </div>

            <div class="field">
              <label for="year">Year of Study</label>
              <select id="year" required>
                <option value="">Select Year</option>
                <option value="1">1st Year</option>
                <option value="2">2nd Year</option>
                <option value="3">3rd Year</option>
                <option value="4">4th Year</option>
              </select>
            </div>

            <div class="field">
              <label for="track">Project Track</label>
              <select id="track" required>
                <option value="">Select Track</option>
                <option value="education">Education</option>
                <option value="entertainment">Entertainment</option>
                <option value="ai-agents">AI Agents & Automations</option>
                <option value="core-ai">Core AI & ML</option>
                <option value="big-data">Big Data</option>
                <option value="cutting-edge">Cutting-edge Agents</option>
              </select>
            </div>
            
            <div class="field full-width">
              <label for="project-name">Project Name (Idea)</label>
              <input type="text" id="project-name" required>
            </div>

            <div class="field full-width checkbox-field">
              <label class="checkbox-container">
                <input type="checkbox" id="declaration" required>
                <span class="checkmark"></span>
                <span class="label-text">
                  I hereby declare that all my details are correct and I will follow all the rules and regulations by the organization. Any wrong behavior in the WhatsApp group will disqualify the team.
                </span>
              </label>
            </div>
          </div>
          
          <div class="actions">
            <button type="button" class="back-btn">Back</button>
            <button type="submit" class="submit-btn">Proceed to Payment</button>
          </div>
        </form>
      </div>

      <!-- Step 3: Redirecting to Payment -->
      <div id="step-3" class="step">
        <div class="payment-card reveal-animation" style="text-align: center; padding: 3rem 2rem;">
          <div style="font-size: 3rem; margin-bottom: 1rem;">🔄</div>
          <h2>Redirecting to Payment...</h2>
          <p style="color: #64748b; margin: 1rem 0;">You will be redirected to the secure Zoho payment page. Please complete your payment there.</p>
          <p class="amount-display">Amount: <span id="display-payment-amount">₹299</span></p>
          <div id="payment-error-msg" class="error-msg" style="display: none;">
            <p id="payment-error-text">Something went wrong.</p>
            <button type="button" class="retry-btn" id="retry-payment-btn">Retry Payment</button>
          </div>
          <div class="actions" style="justify-content: center;">
            <button type="button" class="cancel-btn" id="cancel-payment-btn">Cancel</button>
          </div>
        </div>
      </div>

      <!-- Step 5: Success & Invoice & WhatsApp Join -->
      <div id="step-5" class="step">
        <div class="success-message" style="text-align: center; padding: 1.5rem 0;">
          <div class="success-icon">🎉</div>
          <h2 style="margin-top: 1rem; color: #10b981;">Payment Successful!</h2>
          <p style="color: #64748b; margin-top: 0.5rem; font-size: 1.1rem;">Your registration is complete.</p>
          
          <div class="code-box">
             <span id="final-generated-code">IND-2026-XXXXX</span>
             <small style="display: block; margin-top: 0.5rem; color: #64748b;">Participation Code</small>
          </div>

          <div class="action-card">
            <h3>Join WhatsApp Group</h3>
            <p>Stay updated with event alerts and announcements.</p>
            <div class="action-buttons">
              <a href="https://chat.whatsapp.com/G5v7tXGzX7z7z7z7z7z7" target="_blank" class="whatsapp-btn">Join Official Group</a>
              <button type="button" class="invoice-btn" id="download-invoice-btn">Download Invoice (PDF)</button>
            </div>
          </div>
          
          <div class="actions" style="margin-top: 2rem; justify-content: center;">
             <button type="button" class="done-btn" onclick="document.querySelector('#registration-modal .close-btn').click();">Close</button>
          </div>
        </div>
      </div>
    </div>

    <!-- ALREADY REGISTERED FLOW -->
    <div id="already-registered" class="tab-content">
      <div class="lookup-section">
        <h2>Find Your Registration</h2>
        <p>Enter your unique code (e.g., IND-2026-XXXXX or TEAM-2026-XXXXX) to view your details.</p>
        <div class="lookup-form">
          <input type="text" id="lookup-code" class="lookup-input">
          <button type="button" id="lookup-btn" class="submit-btn" style="width: auto;">Search</button>
        </div>
        <p id="lookup-error" style="color: #ef4444; margin-top: 1rem; display: none;">Invalid Code. Please check and try again.</p>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // DOM Elements
    const modal = document.getElementById('registration-modal');
    const overlay = modal?.querySelector('.modal-overlay');
    const closeBtn = modal?.querySelector('.close-btn');
    const typeBtns = modal?.querySelectorAll('.type-btn');
    const step1 = document.getElementById('step-1');
    const step2 = document.getElementById('step-2');
    const step3 = document.getElementById('step-3');
    const step5 = document.getElementById('step-5');
    const backBtn = document.querySelector('.back-btn');
    const form = document.getElementById('reg-form');
    const teamNameField = document.getElementById('team-name-field');
    const formTitle = document.getElementById('form-title');
    const tabBtns = document.querySelectorAll('.tab-btn');
    const tabContents = document.querySelectorAll('.tab-content');

    // Payment Display Elements
    const displayAmount = document.getElementById('display-payment-amount');
    const downloadInvoiceBtn = document.getElementById('download-invoice-btn');

    let registrationType = 'individual';
    let registrationData: any = {};

    // Handle scroll locking with Lenis
    if (modal) {
      const observer = new MutationObserver((mutations) => {
        mutations.forEach((mutation) => {
          if (mutation.type === 'attributes' && mutation.attributeName === 'data-open') {
            const isOpen = modal.dataset.open === 'true';
            modal.setAttribute('aria-hidden', isOpen ? 'false' : 'true');

            if (isOpen) {
              document.body.classList.add('scroll-locked');
              if ((window as any).lenis) (window as any).lenis.stop();
            } else {
              document.body.classList.remove('scroll-locked');
              if ((window as any).lenis) (window as any).lenis.start();
            }
          }
        });
      });
      observer.observe(modal, { attributes: true });
    }

    // Close Modal Function
    function closeModal() {
      if (modal) {
        modal.dataset.open = "false";
        modal.setAttribute('aria-hidden', 'true');
        setTimeout(() => {
          resetForm();
        }, 400);
      }
    }

    if (overlay) overlay.addEventListener('click', closeModal);
    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && modal?.dataset.open === "true") {
        closeModal();
      }
    });

    // Tab Switching Logic
    tabBtns?.forEach(btn => {
      btn.addEventListener('click', () => {
        tabBtns.forEach(b => b.classList.remove('active'));
        tabContents?.forEach(c => c.classList.remove('active'));
        btn.classList.add('active');
        // @ts-ignore
        const target = btn.dataset.target;
        const content = document.getElementById(target);
        if (content) content.classList.add('active');
      });
    });

    // Step 1: Selection
    typeBtns?.forEach(btn => {
      btn.addEventListener('click', () => {
        // @ts-ignore
        registrationType = btn.dataset.type;
        
        const formElements = form?.elements;
        if(formElements) {
            for(let i=0; i<formElements.length; i++) {
                (formElements[i] as HTMLInputElement).disabled = false;
            }
        }
        const submitBtn = form?.querySelector('.submit-btn') as HTMLButtonElement;
        if (submitBtn) submitBtn.style.display = 'block';
        if (form) (form as HTMLFormElement).reset();
        
        if (registrationType === 'team') {
          if(teamNameField) teamNameField.style.display = 'block';
          if(formTitle) formTitle.textContent = 'Team Registration';
          document.getElementById('team-name')?.setAttribute('required', 'true');
          document.querySelectorAll('.team-member-field')?.forEach(el => {
            (el as HTMLElement).style.display = 'flex';
          });
          const nameLabel = document.querySelector('label[for="full-name"]');
          if(nameLabel) nameLabel.textContent = "Team Lead Name";
          const emailLabel = document.querySelector('label[for="email"]');
          if(emailLabel) emailLabel.textContent = "Team Lead Email (Main ID)";
        } else {
          if(teamNameField) teamNameField.style.display = 'none';
          if(formTitle) formTitle.textContent = 'Individual Registration';
          document.getElementById('team-name')?.removeAttribute('required');
          document.querySelectorAll('.team-member-field')?.forEach(el => {
            (el as HTMLElement).style.display = 'none';
          });
          const nameLabel = document.querySelector('label[for="full-name"]');
          if(nameLabel) nameLabel.textContent = "Full Name";
          const emailLabel = document.querySelector('label[for="email"]');
          if(emailLabel) emailLabel.textContent = "Email ID";
        }

        step1?.classList.remove('active');
        step2?.classList.add('active');
      });
    });

    backBtn?.addEventListener('click', () => {
      step2?.classList.remove('active');
      step1?.classList.add('active');
    });

    // Step 2: Proceed to Payment (Zoho Payment Link)
    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const submitBtn = form?.querySelector('.submit-btn') as HTMLButtonElement;
      if (submitBtn) {
        submitBtn.disabled = true;
        submitBtn.textContent = 'Processing...';
      }

      // Store current form data
      registrationData = {
        fullName: (document.getElementById('full-name') as HTMLInputElement).value,
        email: (document.getElementById('email') as HTMLInputElement).value,
        phone: (document.getElementById('phone') as HTMLInputElement).value,
        college: (document.getElementById('college') as HTMLInputElement).value,
        department: (document.getElementById('dept') as HTMLInputElement).value,
        year: (document.getElementById('year') as HTMLSelectElement).value,
        track: (document.getElementById('track') as HTMLSelectElement).value,
        projectName: (document.getElementById('project-name') as HTMLInputElement).value,
        teamName: registrationType === 'team' ? (document.getElementById('team-name') as HTMLInputElement).value : '',
        members: [
          (document.getElementById('member-2') as HTMLInputElement).value,
          (document.getElementById('member-3') as HTMLInputElement).value,
          (document.getElementById('member-4') as HTMLInputElement).value
        ]
      };

      // Save data to sessionStorage so it persists after Zoho redirect
      sessionStorage.setItem('codekar_reg_data', JSON.stringify(registrationData));
      sessionStorage.setItem('codekar_reg_type', registrationType);

      const amount = registrationType === 'team' ? 999 : 299;
      if (displayAmount) displayAmount.textContent = `₹${amount}`;

      // Show loading step
      step2?.classList.remove('active');
      step3?.classList.add('active');

      // Call Zoho Payment Handler Edge Function
      try {
        const { supabase } = await import('../../lib/supabase');
        const isLocal = window.location.hostname === 'localhost' || window.location.hostname.match(/\d+\.\d+\.\d+\.\d+/);
        const returnUrl = isLocal 
          ? `${window.location.origin}${window.location.pathname}?status=success`
          : `https://www.codekarx.com${window.location.pathname}?status=success`;

        const { data, error } = await supabase.functions.invoke('zoho-payment-handler', {
          body: {
            action: 'create-link',
            amount: amount,
            customer_name: registrationData.teamName || registrationData.fullName,
            order_id: `codekar_${Date.now()}`,
            email: registrationData.email,
            return_url: returnUrl
          },
        });

        if (error || !data?.payment_url) {
          console.error('Payment link error:', { error, data });
          throw new Error(data?.error || error?.message || 'Failed to create payment link');
        }

        // Redirect to Zoho payment page
        window.location.href = data.payment_url;

      } catch (err: any) {
        console.error('Payment flow error:', err);
        const errorMsg = document.getElementById('payment-error-msg');
        const errorText = document.getElementById('payment-error-text');
        if (errorMsg) errorMsg.style.display = 'block';
        if (errorText) errorText.textContent = err.message || 'Failed to create payment link. Please try again.';
        
        if (submitBtn) {
          submitBtn.disabled = false;
          submitBtn.textContent = 'Proceed to Payment';
        }
      }
    });

    // Cancel payment redirect
    const cancelPaymentBtn = document.getElementById('cancel-payment-btn');
    const retryPaymentBtn = document.getElementById('retry-payment-btn');
    
    cancelPaymentBtn?.addEventListener('click', () => {
      step3?.classList.remove('active');
      step2?.classList.add('active');
      const submitBtn = form?.querySelector('.submit-btn') as HTMLButtonElement;
      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Proceed to Payment';
      }
    });

    retryPaymentBtn?.addEventListener('click', () => {
      const errorMsg = document.getElementById('payment-error-msg');
      if (errorMsg) errorMsg.style.display = 'none';
      // Re-submit the form
      form?.dispatchEvent(new Event('submit'));
    });

    // ============================================
    // HANDLE RETURN FROM ZOHO PAYMENT
    // ============================================
    const urlParams = new URLSearchParams(window.location.search);
    const paymentStatus = urlParams.get('status');

    if (paymentStatus === 'success') {
      // Recover saved registration data
      const savedData = sessionStorage.getItem('codekar_reg_data');
      const savedType = sessionStorage.getItem('codekar_reg_type');

      if (savedData && savedType) {
        // Auto-open modal and finalize
        if (modal) modal.dataset.open = 'true';
        
        // Show a processing state
        step1?.classList.remove('active');
        step2?.classList.remove('active');
        step3?.classList.remove('active');
        step5?.classList.remove('active');
        
        // Create a temporary "Finalizing" state using step3
        if (step3) {
          step3.classList.add('active');
          step3.innerHTML = `
            <div class="payment-card reveal-animation" style="text-align: center; padding: 3rem 2rem;">
              <div style="font-size: 3rem; margin-bottom: 1rem;">✅</div>
              <h2>Payment Received!</h2>
              <p style="color: #64748b; margin: 1rem 0;">Finalizing your registration...</p>
            </div>
          `;
        }

        const parsedData = JSON.parse(savedData);
        registrationType = savedType;

        // Finalize DB insert
        (async () => {
          try {
            const { supabase } = await import('../../lib/supabase');

            let uniqueCode = '';
            const randomStr = () => Math.random().toString(36).substring(2, 7).toUpperCase();
            let isUnique = false;
            let attempts = 0;

            while (!isUnique && attempts < 5) {
              uniqueCode = registrationType === 'team' ? `TEAM-2026-${randomStr()}` : `IND-2026-${randomStr()}`;
              const { data, error } = await supabase.from('registrations').select('id').eq('generated_code', uniqueCode);
              if (error) throw error;
              if (data && data.length === 0) isUnique = true;
              attempts++;
            }

            const insertData = {
              registration_type: registrationType,
              generated_code: uniqueCode,
              college: parsedData.college,
              department: parsedData.department,
              year_of_study: parseInt(parsedData.year),
              track: parsedData.track,
              project_name: parsedData.projectName,
              full_name: parsedData.fullName,
              email: parsedData.email,
              phone: parsedData.phone,
              team_name: parsedData.teamName || null,
              team_leader_name: parsedData.fullName,
              leader_email: parsedData.email,
              leader_phone: parsedData.phone,
              member_2_name: parsedData.members?.[0] || null,
              member_3_name: parsedData.members?.[1] || null,
              member_4_name: parsedData.members?.[2] || null,
              transaction_id: `zoho_${Date.now()}`,
              payer_phone: parsedData.phone,
              payment_status: 'Paid',
              amount: registrationType === 'team' ? 999 : 299
            };

            const { error: insertError } = await supabase.from('registrations').insert([insertData]);
            if (insertError) throw insertError;

            // Send confirmation email
            try {
              supabase.functions.invoke('send-confirmation-email', {
                body: {
                  name: parsedData.teamName || parsedData.fullName,
                  email: parsedData.email,
                  unique_code: uniqueCode
                }
              });
            } catch (emailErr) {}

            // Clear saved data
            sessionStorage.removeItem('codekar_reg_data');
            sessionStorage.removeItem('codekar_reg_type');

            // Clean URL params
            window.history.replaceState({}, document.title, window.location.pathname);

            // Show success step
            const finalCodeDisplay = document.getElementById('final-generated-code');
            if (finalCodeDisplay) finalCodeDisplay.textContent = uniqueCode;

            step3?.classList.remove('active');
            step5?.classList.add('active');

          } catch (err: any) {
            console.error('Finalization error:', err);
            if (step3) {
              step3.innerHTML = `
                <div class="payment-card" style="text-align: center; padding: 3rem 2rem;">
                  <div style="font-size: 3rem; margin-bottom: 1rem;">⚠️</div>
                  <h2>Registration Error</h2>
                  <p style="color: #ef4444; margin: 1rem 0;">${err.message || 'Failed to save registration. Please contact support.'}</p>
                  <p style="color: #64748b;">Your payment was successful. Please contact the organizers with your email for assistance.</p>
                </div>
              `;
            }
          }
        })();
      }
    }

    // Invoice Generation
    downloadInvoiceBtn?.addEventListener('click', async () => {
      const { jsPDF } = await import('jspdf');
      const doc = new jsPDF();
      const invoiceId = `INV-${Math.random().toString(36).substring(2, 9).toUpperCase()}`;
      const now = new Date();
      
      // Design Constants
      const primaryColor = [37, 99, 235];
      const grayColor = [100, 116, 139];
      
      // Header
      doc.setFontSize(22);
      doc.setTextColor(primaryColor[0], primaryColor[1], primaryColor[2]);
      doc.text('Unai Tech', 105, 25, { align: 'center' });
      
      doc.setFontSize(10);
      doc.setTextColor(grayColor[0], grayColor[1], grayColor[2]);
      doc.text('Payment Received Successfully', 105, 32, { align: 'center' });

      // Badge/Line
      doc.setDrawColor(primaryColor[0], primaryColor[1], primaryColor[2]);
      doc.setLineWidth(0.5);
      doc.line(20, 40, 190, 40);

      // Invoice Details
      doc.setTextColor(0, 0, 0);
      doc.setFontSize(11);
      doc.text(`Invoice ID: ${invoiceId}`, 20, 55);
      doc.text(`Date & Time: ${now.toLocaleString()}`, 20, 62);
      doc.text(`Status: Paid`, 20, 69);

      // Registration Details
      doc.setFontSize(14);
      doc.setTextColor(primaryColor[0], primaryColor[1], primaryColor[2]);
      doc.text('Details', 20, 85);
      
      doc.setTextColor(0, 0, 0);
      doc.setFontSize(11);
      doc.text(`Registration Type: ${registrationType.charAt(0).toUpperCase() + registrationType.slice(1)}`, 20, 95);
      doc.text(`Team Name: ${registrationData.teamName || registrationData.fullName}`, 20, 102);
      doc.text(`Amount Paid: ₹${registrationType === 'team' ? '999' : '299'}`, 20, 109);
      doc.text(`Transaction: Zoho Payment`, 20, 116);
      doc.text(`Email: ${registrationData.email || 'N/A'}`, 20, 123);
      doc.text(`Participant Code: ${document.getElementById('final-generated-code')?.textContent || 'N/A'}`, 20, 130);

      // Footer
      doc.setDrawColor(226, 232, 240);
      doc.line(20, 260, 190, 260);
      doc.setFontSize(9);
      doc.setTextColor(grayColor[0], grayColor[1], grayColor[2]);
      doc.text('This is a computer-generated invoice. No signature required.', 105, 270, { align: 'center' });
      doc.text('Thank you for participating!', 105, 275, { align: 'center' });

      doc.save(`${invoiceId}.pdf`);
    });

    function resetForm() {
      step1?.classList.add('active');
      step2?.classList.remove('active');
      step3?.classList.remove('active');
      step5?.classList.remove('active');
      
      const formElements = form?.elements;
      if(formElements) {
          for(let i=0; i<formElements.length; i++) {
              (formElements[i] as HTMLInputElement).disabled = false;
          }
      }
      const submitBtn = form?.querySelector('.submit-btn') as HTMLButtonElement;
      if (submitBtn) {
          submitBtn.style.display = 'block';
          submitBtn.textContent = 'Proceed to Payment';
          submitBtn.disabled = false;
      }
      
      // @ts-ignore
      form?.reset();
    }
  });

</script>

<style>
  .modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 2000;
    display: flex;
    justify-content: center;
    align-items: center;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.3s, visibility 0.3s;
  }

  .modal[data-open="true"] {
    opacity: 1;
    visibility: visible;
    pointer-events: all;
  }

  .modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.6);
    backdrop-filter: blur(4px);
  }

  .modal-content {
    background: white;
    width: 90%;
    max-width: 650px;
    padding: 2rem;
    border-radius: 1rem;
    position: relative;
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    transform: translateY(20px);
    transition: transform 0.3s;
    max-height: 90vh;
    max-height: 90dvh;
    overflow-y: auto;
    overflow-x: hidden;
    overscroll-behavior: contain;
    -webkit-overflow-scrolling: touch;
    scroll-behavior: smooth;
    scrollbar-width: thin;
    scrollbar-color: #cbd5e1 transparent;
    pointer-events: auto;
    touch-action: manipulation;
    -webkit-tap-highlight-color: transparent;
  }

  .modal-content::-webkit-scrollbar {
    width: 6px;
  }
  .modal-content::-webkit-scrollbar-track {
    background: transparent;
  }
  .modal-content::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 10px;
  }

  .modal[data-open="true"] .modal-content {
    transform: translateY(0);
  }

  .close-btn {
    position: absolute;
    top: 1rem;
    right: 1.5rem;
    background: none;
    border: none;
    font-size: 2rem;
    line-height: 1;
    color: #64748b;
    padding: 0.5rem;
    cursor: pointer;
    transition: color 0.2s;
    z-index: 10;
    max-width: 40px;
  }
  
  .close-btn:hover { color: #0f172a; }
  .close-btn:active { transform: scale(0.9); }

  .modal-tabs {
    display: flex;
    gap: 1rem;
    margin-bottom: 2rem;
    border-bottom: 2px solid #e2e8f0;
    padding-bottom: 0.5rem;
  }

  .tab-btn {
    background: none;
    border: none;
    font-size: 1.1rem;
    font-weight: 600;
    color: #64748b;
    cursor: pointer;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    transition: all 0.2s;
  }

  .tab-btn:hover {
    background: #f1f5f9;
  }

  .tab-btn:active {
    transform: scale(0.95);
  }

  .tab-btn.active {
    color: var(--color-s-primary, #2563eb);
    background: #eff6ff;
    transform: scale(1.05);
  }

  .tab-content {
    display: none;
    animation: fadeIn 0.4s;
  }
  
  .tab-content.active {
    display: block;
  }

  .step { 
    display: none; 
    animation: fadeIn 0.4s; 
    padding-bottom: 2rem; /* Ensure last elements are fully scrollable on mobile */
  }
  .step.active { display: block; }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .options {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .type-btn {
    background: #f1f5f9;
    border: 2px solid transparent;
    border-radius: 1rem;
    padding: 2rem;
    text-align: center;
    transition: all 0.2s;
    cursor: pointer;
  }
  .type-btn:hover {
    border-color: var(--color-s-primary, #2563eb);
    background: #e0f2fe;
  }
  .type-btn:active {
    transform: scale(0.98);
  }
  .icon { font-size: 3rem; display: block; margin-bottom: 1rem; }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
    margin-top: 1.5rem;
  }
  .field { display: flex; flex-direction: column; gap: 0.5rem; }
  .full-width { grid-column: span 2; }

  .checkbox-field { margin-top: 1rem; }
  .checkbox-container {
    display: flex; align-items: flex-start; gap: 0.75rem;
    cursor: pointer; font-size: 0.875rem; color: #475569; position: relative;
  }
  .checkbox-container input { position: absolute; opacity: 0; cursor: pointer; height: 0; width: 0; }
  .checkmark {
    position: relative; height: 20px; width: 20px; background-color: #f8fafc;
    border: 1px solid #e2e8f0; border-radius: 4px; flex-shrink: 0; margin-top: 2px;
  }
  .checkbox-container input:checked ~ .checkmark { background-color: var(--color-s-primary, #2563eb); }
  .checkbox-container input:checked ~ .checkmark:after {
    content: ""; position: absolute; left: 6px; top: 2px; width: 5px; height: 10px;
    border: solid white; border-width: 0 2px 2px 0; transform: rotate(45deg);
  }

  label { font-size: 0.875rem; font-weight: 500; color: #475569; }
  input, select {
    padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 0.5rem; background: #f8fafc;
    font-size: 16px; /* Prevents iOS auto-zoom on focus */
  }
  input:focus, select:focus {
    outline: none; border-color: var(--color-s-primary, #2563eb);
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }
  input:disabled, select:disabled {
    opacity: 0.7; cursor: not-allowed; background: #e2e8f0;
  }

  button:active, .submit-btn:active, .back-btn:active, .cancel-btn:active, .whatsapp-btn:active, .invoice-btn:active, .retry-btn:active {
    transform: scale(0.98);
    opacity: 0.9;
  }

  .actions { margin-top: 2rem; display: flex; justify-content: space-between; }
  .submit-btn {
    background: var(--color-s-primary, #2563eb); color: white; border: none;
    padding: 0.75rem 2rem; border-radius: 0.5rem; font-weight: 600; cursor: pointer;
    transition: opacity 0.2s;
  }
  .submit-btn:hover { opacity: 0.9; }
  .submit-btn:disabled { opacity: 0.5; cursor: not-allowed; }

  .back-btn {
    background: transparent; color: var(--color-s-primary, #2563eb);
    border: 2px solid #e2e8f0; padding: 0.75rem 1.5rem; border-radius: 0.5rem;
    font-weight: 600; cursor: pointer; display: inline-flex; align-items: center; gap: 0.5rem;
  }
  .back-btn:hover { background: #f8fafc; border-color: var(--color-s-primary, #2563eb); }

  /* Success Step */
  /* Payment Card Styles */
  .payment-card {
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 1.5rem;
    padding: 2rem;
    position: relative;
    overflow: hidden;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    text-align: center;
  }

  .timer-badge {
    position: absolute;
    top: 1rem;
    right: 1.5rem;
    background: #fee2e2;
    color: #ef4444;
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-weight: 700;
    font-size: 0.9rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    border: 1px solid #fecaca;
  }

  .payment-card h2 {
    font-size: 1.5rem;
    margin-top: 1rem;
    color: #0f172a;
  }

  .qr-container {
    background: white;
    width: 200px;
    height: 200px;
    margin: 1.5rem auto;
    padding: 0.5rem;
    border-radius: 1rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    display: flex;
    justify-content: center;
    align-items: center;
    border: 1px solid #f1f5f9;
  }

  .qr-container img {
    max-width: 100%;
    border-radius: 0.5rem;
  }

  .amount-display {
    font-size: 2rem;
    font-weight: 800;
    color: var(--color-s-primary, #2563eb);
    margin: 1rem 0;
  }

  .payment-instructions {
    font-size: 0.9rem;
    color: #64748b;
    line-height: 1.5;
    margin-bottom: 1.5rem;
  }

  .reveal-animation {
    animation: revealScale 0.6s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  @keyframes revealScale {
    0% { opacity: 0; transform: scale(0.9) translateY(20px); }
    100% { opacity: 1; transform: scale(1) translateY(0); }
  }

  .error-msg {
    margin: 1.5rem 0;
    padding: 1rem;
    background: #fff1f2;
    border: 1px solid #fda4af;
    border-radius: 0.75rem;
    color: #be123c;
  }

  .retry-btn {
    margin-top: 0.5rem;
    background: #ef4444;
    color: white;
    border: none;
    padding: 0.5rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    cursor: pointer;
  }

  .cancel-btn {
    background: white;
    color: #64748b;
    border: 1px solid #e2e8f0;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    cursor: pointer;
  }

  .cancel-btn:hover {
    background: #f1f5f9;
  }

  /* Success Action Card */
  .action-card {
    background: #f0f9ff;
    border: 1px solid #bae6fd;
    border-radius: 1rem;
    padding: 1.5rem;
    margin-top: 2rem;
    text-align: left;
  }

  .action-card h3 {
    font-size: 1.1rem;
    color: #0369a1;
    margin-bottom: 0.5rem;
  }

  .action-card p {
    font-size: 0.9rem;
    color: #0c4a6e;
    margin-bottom: 1rem;
  }

  .action-buttons {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .whatsapp-btn {
    background: #25d366;
    color: white;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 700;
    text-align: center;
    transition: transform 0.2s;
  }

  .whatsapp-btn:hover {
    transform: translateY(-2px);
    background: #22c55e;
  }

  .invoice-btn {
    background: white;
    color: #0369a1;
    border: 1px solid #bae6fd;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    cursor: pointer;
  }

  .invoice-btn:hover {
    background: #e0f2fe;
  }

  .done-btn {
    background: #1e293b;
    color: white;
    border: none;
    padding: 0.75rem 2.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    cursor: pointer;
  }

  .success-icon {
    font-size: 4rem; animation: bounce 1s;
  }
  .code-box {
    margin: 1rem auto;
    padding: 1.25rem;
    background: #f1f5f9;
    border: 2px dashed #94a3b8;
    border-radius: 0.75rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    max-width: 400px;
  }
  #generated-code-display {
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 2px;
    color: #0f172a;
  }
  .copy-btn {
    background: white; border: 1px solid #cbd5e1; padding: 0.5rem 1rem;
    border-radius: 0.5rem; cursor: pointer; font-weight: 600; transition: all 0.2s;
  }
  .copy-btn:hover { background: #f8fafc; border-color: var(--color-s-primary, #2563eb); }
  .done-btn {
    background: #0f172a; color: white; border: none; padding: 0.75rem 2.5rem;
    border-radius: 0.5rem; font-weight: 600; cursor: pointer;
  }

  /* Lookup Section */
  .lookup-section {
    text-align: center;
    padding: 2rem 0;
  }
  .lookup-form {
    margin-top: 1.5rem;
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
  }
  .lookup-input {
    width: 60%;
    font-size: 1.1rem;
    text-transform: uppercase;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-20px); }
    60% { transform: translateY(-10px); }
  }

  @media (max-width: 640px) {
    .options { grid-template-columns: 1fr; }
    .form-grid { grid-template-columns: 1fr; }
    .full-width { grid-column: auto; }
    .modal-content {
      width: 100%; height: 100dvh; max-height: 100dvh; border-radius: 0; padding: 1.5rem;
    }
    .lookup-form { flex-direction: column; }
    .lookup-input { width: 100%; }
    .modal-tabs { flex-direction: column; }
  }
</style>
